<p><%= @resource.email %> 고객님이 요청하신 비밀번호 변경 안내 메일입니다.</p>
<p>아래 링크에서 비밀번호 변경 가능합니다.</p>

<p><%= link_to '비밀번호 변경 페이지', edit_password_url(@resource, reset_password_token: @token, config: message['client-config'].to_s, redirect_url: message['redirect-url'].to_s).html_safe %></p>

<p>변경 후 다시 로그인 해보시기 바랍니다.</p>
<p>감사합니다.</p>
